# ── Core ─────────────────────────────────────────────────────────
# The RAG engine runs on the Python standard library alone. numpy is an
# optional acceleration for similarity search; without it the store uses
# a pure-Python cosine implementation.
numpy>=1.24.0

# ── API layer ────────────────────────────────────────────────────
fastapi>=0.104.0
uvicorn>=0.24.0
pydantic>=2.4.0
python-multipart>=0.0.6    # file uploads for /rag/ingest/upload

# ── Chatbot UI ───────────────────────────────────────────────────
streamlit>=1.28.0

# ── HTTP to Ollama (optional) ────────────────────────────────────
# The engine falls back to stdlib urllib if httpx is absent.
httpx>=0.25.0

# ── Cloud answer generation (optional) ───────────────────────────
# Enables written answers via the Claude API with no local model
# download — set ANTHROPIC_API_KEY and GENERATION_PROVIDER="auto".
# Without it, generation uses Ollama or the extractive fallback.
anthropic>=0.40.0

# ── Cloud semantic search (optional) ─────────────────────────────
# Meaning-based retrieval with no local model — set VOYAGE_API_KEY and
# RAG_EMBED_PROVIDER="voyage" (or "auto"). Without it (and without
# Ollama), retrieval uses the local keyword embedding.
voyageai>=0.2.0

# ── Document parsing (optional) ──────────────────────────────────
# Only needed to index PDF / Word documents; text/markdown/CSV work
# without them. Missing parsers cause those file types to be skipped
# with a warning, never a crash.
pypdf>=4.0.0
python-docx>=1.1.0
